{
	"name": "toolbox",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"lint": "tsc --noEmit && prettier --check . && eslint .",
		"lint:fix": "tsc --noEmit && prettier --write . && eslint . --fix",
		"test": "bun test"
	},
	"devDependencies": {
		"@types/bun": "^1.2.2",
		"eslint": "^9.20.1",
		"eslint-config-love": "^118.0.0",
		"memfs": "^4.17.0",
		"prettier": "^3.5.1",
		"unionfs": "^4.5.4"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
